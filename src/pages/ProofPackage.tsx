import { LockedFeature } from "@/components/taura/LockedFeature";

const ProofPackage = () => (
  <LockedFeature
    featureName="Proof Package"
    description="Genera pacchetti di rendicontazione per brand e sponsor: screenshot dei post, metriche di engagement, recap campagna e report in PDF."
  />
);

export default ProofPackage;
